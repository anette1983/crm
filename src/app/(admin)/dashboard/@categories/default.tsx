import React from 'react';

type Props = {};

function Default({}: Props) {
  return null;
}

// Якщо некст джс не може відновити активний стан слота на основі поточної урл адреси, можемо використати спец файл, який буде рендерити запасний варіант
export default Default;
